country_decisions = {
    zhx_open_tianxia_overview = {
        major = yes

        potential = {
            has_global_flag = zhx_system_initialised_v3
            zhx_can_see_tianxia_gui = yes
        }

        allow = {
            always = yes
        }

        effect = {
            custom_tooltip = zhx_open_tianxia_overview_tt
            hidden_effect = {
                # 这是右下角顶栏按钮以外的备用入口；这里只切换显示状态，
                # 不占用原版中华帝国、神罗、教廷等硬编码界面。
                set_country_flag = zhx_tianxia_gui_visible
                zhx_build_gui_roster = yes
            }
        }

        ai_will_do = { factor = 0 }
    }

    # EU4 cannot register a new native map mode. Hovering this persistent,
    # read-only decision uses the native decision highlighter as a safe proxy.
    zhx_view_tianxia_extent = {
        major = yes

        potential = {
            has_global_flag = zhx_system_initialised_v3
            zhx_can_see_tianxia_gui = yes
        }

        provinces_to_highlight = {
            owner = {
                zhx_is_tianxia_polity = yes
            }
        }

        allow = {
            custom_trigger_tooltip = {
                tooltip = zhx_view_tianxia_extent_read_only_tt
                always = no
            }
        }

        effect = {
        }

        ai_will_do = {
            factor = 0
        }
    }

    # A player-facing authority operation. The cooldown is stored as a normal
    # country modifier so it survives saves and needs no date arithmetic.
    zhx_convene_grand_court = {
        major = yes

        potential = {
            has_global_flag = zhx_system_initialised_v3
            zhx_is_tianzi = yes
        }

        allow = {
            is_at_war = no
            stability = 0
            treasury = 100
            dip_power = 50
            NOT = { has_country_modifier = zhx_grand_court_cooldown }
        }

        effect = {
            custom_tooltip = zhx_convene_grand_court_effect_tt
            hidden_effect = {
                add_treasury = -100
                add_dip_power = -50
                change_variable = {
                    which = zhx_ritual_authority
                    value = 10
                }
                zhx_clamp_ritual_authority = yes
                zhx_refresh_ritual_order = yes
                add_country_modifier = {
                    name = zhx_grand_court_cooldown
                    duration = 1825
                }
                country_event = { id = zhx_system.4 }
            }
        }

        ai_will_do = {
            factor = 0
        }
    }

    # The first scripted covenant is the vertical slice for an HRE-style
    # reform: authority cost, one-time global state and a shared member effect.
    zhx_reaffirm_the_covenant = {
        major = yes

        potential = {
            has_global_flag = zhx_system_initialised_v3
            zhx_is_tianzi = yes
            NOT = { has_global_flag = zhx_reform_reaffirmed_covenant }
        }

        allow = {
            custom_trigger_tooltip = {
                tooltip = zhx_reaffirm_the_covenant_authority_tt
                check_variable = {
                    which = zhx_ritual_authority
                    value = 50
                }
            }
            stability = 1
            is_at_war = no
        }

        effect = {
            custom_tooltip = zhx_reaffirm_the_covenant_effect_tt
            hidden_effect = {
                change_variable = {
                    which = zhx_ritual_authority
                    value = -50
                }
                set_global_flag = zhx_reform_reaffirmed_covenant
                every_country = {
                    limit = { zhx_is_tianxia_polity = yes }
                    add_country_modifier = {
                        name = zhx_reaffirmed_covenant_modifier
                        duration = -1
                    }
                }
                zhx_clamp_ritual_authority = yes
                zhx_refresh_ritual_order = yes
                country_event = { id = zhx_system.5 }
            }
        }

        ai_will_do = {
            factor = 0
        }
    }

    # 原版帝国事变允许成员在议论期内表明偏好。这里用三项互斥决议承载改票，
    # 避免要求玩家在事变刚开始的同一天永久锁定立场。
    zhx_vote_support_ritual_breakdown = {
        major = no

        potential = {
            ai = no
            has_global_flag = zhx_system_initialised_v6
            zhx_is_tianxia_member = yes
            NOT = { has_country_flag = zhx_council_vote_a }
            event_target:zhx_tianzi = {
                has_country_flag = zhx_council_phase_ballot_open
                has_country_flag = zhx_council_kind_ritual_breakdown
            }
        }

        allow = { always = yes }

        effect = {
            custom_tooltip = zhx_system.21.a.tt
            hidden_effect = {
                zhx_cast_tianxia_council_vote_a = yes
            }
        }

        ai_will_do = { factor = 0 }
    }

    zhx_vote_oppose_ritual_breakdown = {
        major = no

        potential = {
            ai = no
            has_global_flag = zhx_system_initialised_v6
            zhx_is_tianxia_member = yes
            NOT = { has_country_flag = zhx_council_vote_b }
            event_target:zhx_tianzi = {
                has_country_flag = zhx_council_phase_ballot_open
                has_country_flag = zhx_council_kind_ritual_breakdown
            }
        }

        allow = { always = yes }

        effect = {
            custom_tooltip = zhx_system.21.c.tt
            hidden_effect = {
                zhx_cast_tianxia_council_vote_b = yes
            }
        }

        ai_will_do = { factor = 0 }
    }

    zhx_vote_abstain_ritual_breakdown = {
        major = no

        potential = {
            ai = no
            has_global_flag = zhx_system_initialised_v6
            zhx_is_tianxia_member = yes
            NOT = { has_country_flag = zhx_council_vote_c }
            event_target:zhx_tianzi = {
                has_country_flag = zhx_council_phase_ballot_open
                has_country_flag = zhx_council_kind_ritual_breakdown
            }
        }

        allow = { always = yes }

        effect = {
            custom_tooltip = zhx_system.21.b.tt
            hidden_effect = {
                zhx_cast_tianxia_council_vote_c = yes
            }
        }

        ai_will_do = { factor = 0 }
    }

}
