country_decisions = {
    zhx_apply_to_join_tianxia = {
        major = yes

        potential = {
            has_global_flag = zhx_system_initialised_v14
            exists = yes
            is_subject = no
            NOT = { zhx_is_tianxia_polity = yes }
        }

        allow = {
            custom_trigger_tooltip = {
                tooltip = zhx_apply_to_join_tianxia_requirements_tt
                zhx_can_apply_for_tianxia_membership = yes
                NOT = { has_country_flag = zhx_tianxia_application_pending }
            }
        }

        effect = {
            custom_tooltip = zhx_apply_to_join_tianxia_effect_tt
            hidden_effect = {
                save_event_target_as = zhx_diplomacy_applicant
                set_country_flag = zhx_tianxia_application_pending
                event_target:zhx_tianzi = {
                    country_event = { id = zhx_diplomacy.30 }
                }
            }
        }

        ai_will_do = {
            factor = 0.01
            modifier = {
                factor = 10
                alliance_with = event_target:zhx_tianzi
            }
            modifier = {
                factor = 4
                any_country = {
                    is_enemy = ROOT
                    event_target:zhx_tianzi = { is_enemy = PREV }
                }
            }
            modifier = {
                factor = 0.25
                event_target:zhx_tianzi = { is_rival = ROOT }
            }
        }
    }

    zhx_leave_tianxia = {
        major = yes

        potential = {
            has_global_flag = zhx_system_initialised_v14
            zhx_is_tianxia_member = yes
            NOT = { zhx_is_tianzi = yes }
            is_subject = no
            OR = {
                ai = no
                AND = {
                    ai = yes
                    NOT = { has_opinion = { who = event_target:zhx_tianzi value = -100 } }
                    event_target:zhx_tianzi = { has_country_flag = zhx_ritual_tier_collapsed }
                }
            }
        }

        allow = { is_at_war = no }

        effect = {
            custom_tooltip = zhx_leave_tianxia_effect_tt
            hidden_effect = {
                event_target:zhx_tianzi = {
                    add_opinion = {
                        who = ROOT
                        modifier = zhx_opinion_left_tianxia_tianzi
                    }
                }
                every_country = {
                    limit = {
                        exists = yes
                        zhx_is_tianxia_member = yes
                        NOT = { tag = ROOT }
                    }
                    add_opinion = {
                        who = ROOT
                        modifier = zhx_opinion_left_tianxia_member
                    }
                }
                zhx_remove_tianxia_member = yes
                add_prestige = -25
                add_country_modifier = {
                    name = zhx_tianxia_rejoin_bar
                    duration = 7300
                }
            }
        }

        ai_will_do = { factor = 0.01 }
    }
}
